═══════════════════════════════════════════════════════════════════════════════
VPS QUICK DEPLOYMENT - Copy & Paste These Commands
═══════════════════════════════════════════════════════════════════════════════

STEP 1: On Your Mac (Local Machine)
────────────────────────────────────────────────────────────────────────────

git add .
git commit -m "Fix: Use shared Prisma instance in API routes"
git push origin main


STEP 2: SSH to Your VPS
────────────────────────────────────────────────────────────────────────────

ssh wasiq@your-vps-ip


STEP 3: Run These Commands on VPS (Copy all at once)
────────────────────────────────────────────────────────────────────────────

cd /home/wasiq/resume/Personal-Website-main && \
git pull origin main && \
rm -rf .next && \
npm install && \
npx prisma generate && \
npm run build && \
pm2 stop Wasiq && \
pm2 delete Wasiq && \
pm2 start npm --name "Wasiq" -- run start && \
pm2 save && \
echo "✅ Deployment complete! Checking logs..." && \
sleep 3 && \
pm2 logs Wasiq --lines 30


STEP 4: Verify (Check the logs output)
────────────────────────────────────────────────────────────────────────────

✅ SUCCESS: If you see normal HTTP request logs like:
   GET /api/admin/config 200 in XXms

❌ ERROR: If you still see "Cannot read properties of undefined"
   Then check:
   - Database exists: ls -la prisma/
   - Environment: cat .env
   - Prisma works: npx prisma studio


OPTIONAL: Test the APIs manually
────────────────────────────────────────────────────────────────────────────

curl http://localhost:3000/api/admin/config
curl http://localhost:3000/api/admin/investment-theses


═══════════════════════════════════════════════════════════════════════════════
That's it! Your VPS should now be running the fixed code. 🚀
═══════════════════════════════════════════════════════════════════════════════

